include includes/header         

main(class="my-5 container max-w-[50rem] mx-auto flex flex-col max-lg:bg-[#FFEBC2]/80 rounded px-12")
  header(class="flex items-center rounded py-4 relative after:absolute after:bg-right-bottom after:bg-no-repeat after:bg-[url(https://res.cloudinary.com/bambam/image/upload/v1702684728/w74amqqoyi8bjmycqdyx.svg)] justify-center mt-[7rem] mb-[3rem] text-[64px] font-[kalnia]")
    h1 zeus

  div#copy-info.hidden.absolute.right-10.top-10.py-3.px-5.border.rounded-md
    div copied api key.
  
  div(class="text-24px space-y-5")
    div(class="text-[24px]") hello, 
     span(class="text-[36px] font-[500]")  #{user.name}

    div(class="flex flex-col md:flex-row gap-8 flex-1")
      div(class="w-full border border-[#3D2800] rounded-lg p-3 flex flex-col justify-end") 
        div minions connected 
        div #{user.devices.length}/3

      div(
        class="w-full border border-[#3D2800] rounded-lg p-3 flex flex-col justify-end cursor-pointer" 
        _="on click set bb to #apikey's innerHTML set x to navigator.clipboard.writeText(bb) remove .hidden from #copy-info then wait 3s then add .hidden to #copy-info "
      )  
        div#apikey.hidden #{user.key.key}
        div api key 
        span (click to copy)

      //
        div(class="w-full border border-[#3D2800] rounded-lg p-3 flex flex-col justify-end") 
          a(href="") download latest config. 

  div(class="grid md:grid-cols-2 gap-10 py-6")
    div(class="w-full m-5 mx-auto")
      div(class="p-3 rounded relative") 
        div(class="before:h-[1rem] before:w-full before:bottom-0 before:block before:mt-auto before:absolute before:-inset-1 before:-skew-y-3 before:bg-[#E07A5F]")
          div quick setup::

      ul(class="lg:w-[20rem] mt-5 flex-wrap whitespace-normal text-left mx-auto space-y-[8px] list-[roman]") 
        li copy the api key on your dashbaord.
        li.whitespace-normal 
          a(href="/download") download and install zeus.
        li.whitespace-normal link your computer (minion) to zeus. 
          pre > zeus link api-key
        li to install a package, prepend zeus to your install command.
          pre.whitespace-normal > zeus scoop install -y zig 

    //
      div(class="")
        div(class="p-3 rounded relative") 
          div(class="before:h-[1rem] before:w-full before:bottom-0 before:block before:mt-auto before:absolute before:-inset-1 before:-skew-y-3 before:bg-[#E07A5F]")
            div what to do after downloading your config

        ul(class="lg:w-[20rem] mt-5 flex-wrap whitespace-normal text-left mx-auto space-y-[8px] list-[roman]") 
          li.whitespace-normal  go to your zeus installation 
            pre c:/.zeus 
            or 
            pre /etc/zeus
          li.whitespace-normal  paste the config that you just downloaded.
          li.whitespace-normal  run 
            pre zeus config sync
      
    

include includes/footer         

script(src="https://cdn.tailwindcss.com")
script(src='https://unpkg.com/htmx.org@1.3.1')
script(src="https://unpkg.com/hyperscript.org@0.9.12")
